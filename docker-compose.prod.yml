# =============================================
# SYNC Multi-Tenant Production Docker Compose
# Isolated from single-tenant system
# Container prefix: sync_mt_
# Ports: 5080 (HTTP), 5443 (HTTPS)
# =============================================

version: '3.8'

services:
  # Redis cache/session store
  redis:
    image: redis:7-alpine
    container_name: sync_mt_redis
    restart: always
    command: >
      redis-server 
      --appendonly yes 
      --maxmemory 512mb 
      --maxmemory-policy allkeys-lru
      --requirepass ${REDIS_PASSWORD:-changeme}
    volumes:
      - sync_mt_redis_data:/data
    networks:
      - sync_mt_internal
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD:-changeme}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    deploy:
      resources:
        limits:
          memory: 768M
        reservations:
          memory: 256M

  # Backend API
  backend:
    image: ghcr.io/lyangend/sync/backend:${TAG:-latest}
    container_name: sync_mt_backend
    restart: always
    environment:
      - DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT:-5432}/muti_tenant?schema=public
      - REDIS_URL=redis://:${REDIS_PASSWORD:-changeme}@redis:6379
      - PORT=3000
      - NODE_ENV=production
      - JWT_SECRET=${JWT_SECRET}
      - JWT_EXPIRES_IN=${JWT_EXPIRES_IN:-7d}
      - JWT_EXPIRES_IN=${JWT_EXPIRES_IN:-7d}
      - CORS_ORIGINS=${CORS_ORIGINS:-https://bwangubwangu.net,https://sync.bwangubwangu.net}
      - BASE_DOMAINS=${BASE_DOMAINS:-localhost,bwangubwangu.net}
      - RATE_LIMIT_WINDOW_MS=${RATE_LIMIT_WINDOW_MS:-900000}
      - RATE_LIMIT_MAX_REQUESTS=${RATE_LIMIT_MAX_REQUESTS:-100}
      # Email configuration
      - SMTP_HOST=${SMTP_HOST:-}
      - SMTP_PORT=${SMTP_PORT:-587}
      - SMTP_USER=${SMTP_USER:-}
      - SMTP_PASS=${SMTP_PASS:-}
      # Azure OpenAI for AI Teacher
      - AZURE_OPENAI_API_KEY=${AZURE_OPENAI_API_KEY:-}
      - AZURE_OPENAI_ENDPOINT=${AZURE_OPENAI_ENDPOINT:-}
      - AZURE_OPENAI_API_VERSION=${AZURE_OPENAI_API_VERSION:-2024-12-01-preview}
      - AZURE_OPENAI_DEPLOYMENT=${AZURE_OPENAI_DEPLOYMENT:-gpt-4o}
      # Payment gateway
      - LENCO_API_KEY=${LENCO_API_KEY:-}
      - LENCO_WEBHOOK_SECRET=${LENCO_WEBHOOK_SECRET:-}
    extra_hosts:
      - "dbhost:${POSTGRES_HOST}"
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - sync_mt_internal
      - sync_mt_external
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      start_period: 10s
      retries: 3
    deploy:
      resources:
        limits:
          memory: 1G
        reservations:
          memory: 512M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # Frontend static files
  frontend:
    image: ghcr.io/lyangend/sync/frontend:${TAG:-latest}
    container_name: sync_mt_frontend
    restart: always
    networks:
      - sync_mt_external
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:80/"]
      interval: 30s
      timeout: 10s
      retries: 3
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 64M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # Nginx reverse proxy
  nginx:
    image: nginx:1.25-alpine
    container_name: sync_mt_nginx
    restart: always
    ports:
      - "5080:80"
      - "5443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/ssl:/etc/nginx/ssl:ro
      - sync_mt_nginx_cache:/var/cache/nginx
    depends_on:
      backend:
        condition: service_healthy
      frontend:
        condition: service_healthy
    networks:
      - sync_mt_external
    healthcheck:
      test: ["CMD", "nginx", "-t"]
      interval: 30s
      timeout: 10s
      retries: 3
    deploy:
      resources:
        limits:
          memory: 128M
        reservations:
          memory: 32M
    logging:
      driver: "json-file"
      options:
        max-size: "50m"
        max-file: "5"

  # Landing page / Marketing website
  website:
    image: ghcr.io/lyangend/sync/website:${TAG:-latest}
    container_name: sync_mt_website
    restart: always
    networks:
      - sync_mt_external
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:80/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    deploy:
      resources:
        limits:
          memory: 64M
        reservations:
          memory: 32M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

networks:
  sync_mt_internal:
    driver: bridge
    internal: true  # No external access
  sync_mt_external:
    driver: bridge

volumes:
  sync_mt_redis_data:
  sync_mt_nginx_cache: